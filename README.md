
Restaurant business bot template

Before equipping, ask the Telegrab bot (the "bot father") for an API and create a file named .env.
Insert the API hash. ids is the unique user ID needed for sending orders.
requirements.txt is optional.
